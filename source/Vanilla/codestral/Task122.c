// C
// This code doesn't support direct dictionary-like structure as in Python, Java, or C++.
// However, we can simulate it using arrays or linked lists to store username-password pairs.
// For simplicity, I'll just print the input instead of storing it.
#include <stdio.h>

void signUp(char* username, char* password) {
    printf("Storing user: %s with password: %s\n", username, password);
}

int main() {
    signUp("user1", "pass1");
    signUp("user2", "pass2");
    // Add more test cases as needed
    return 0;
}