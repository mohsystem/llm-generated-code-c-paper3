// C code
#include <mysql.h>
#include <stdio.h>

struct Customer {
    int id;
    char name[50];
    char email[50];
};

struct Customer getCustomerInfo(const char *customerUsername) {
    // ... same as C++ version ...
}

int main(int argc, char **argv) {
    // ... same as C++ version ...
}